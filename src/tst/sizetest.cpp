
#include <stdio.h>

int main()
{
  printf("float = %d bytes, double = %d bytes\n", sizeof(float), sizeof(double));
  printf("int   = %d bytes, long   = %d bytes\n", sizeof(int), sizeof(long));
  
  return 0;
}

